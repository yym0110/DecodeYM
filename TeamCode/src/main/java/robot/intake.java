package robot;

import org.firstinspires.ftc.teamcode.utils.priority.PriorityMotor;

public PriorityMotor intakeMotor;

public class intake {
    //some priority queue
    intakeMotor.//set motor power(setPower(true))
}

public int setPower(boolean intakeRequest){
    int power = 0;
    if(intakeRequest){
        power = 1;
    }
    return power;
}
